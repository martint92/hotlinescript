<% if @reminder.nil? %>
    <p>
        This area is currently blank. Why don't you add something? 
    </p>
    <%= link_to '[new]', new_reminder_path, remote: true %>
<% else %>
    <p>
        <%= @reminder.body.html_safe %>
    </p>
    <%= link_to '[edit]', edit_reminder_path(@reminder), remote: true, class: "edit-buttons" %>
<% end %>