<%= form_for(@verification) do |f| %>
    <%= f.text_field @verification, :placeholder => "Verification", :class => "signup-box-element" %>
    <%= f.submit "Submit", :class => "btn-submit" %>
<% end %>