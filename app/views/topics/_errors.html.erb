<% if topic.errors.any? %>
<div id="error_explanation">
  <h6><%= pluralize(topic.errors.count, "error") %> prohibited this article from being saved:</h2>

  <ul>
  <% topic.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
